{
  "name": "directdine-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root workspace of the DirectDine project",
  "scripts": {
    "backend:dev": "npm run dev --prefix backend",
    "backend:start": "npm run start --prefix backend",
    "customer:dev": "npm run dev --prefix frontend/customer-app",
    "admin:dev": "npm run dev --prefix frontend/admin-app"
  }
}
